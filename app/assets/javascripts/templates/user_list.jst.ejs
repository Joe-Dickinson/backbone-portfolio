 <h2> All Users </h2>

    <ul>
      <% users.each(function(user) { %>
        <li>
          <a href="/users/<%= user.id %>" class="user-link" data-user-id="<%= user.id %>">
            <%= user.get("full_name") %> 
            <br>
          </a>
        </li>
    <% }) %>